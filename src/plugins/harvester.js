var async = require("async");

module.exports = {
    init: function () {
        async.update(async.data.SOURCES);

    },
    getPerfectCreep: function (energyAvailable) {
        
    },
    rateCreeps: function (creeps) {

    }
}